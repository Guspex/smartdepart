intersystems-pyprod>=0.2.0
sentence-transformers>=3.0.0
# ONNX backend, not the sentence-transformers default (torch) -- research.md §22: torch
# reliably segfaulted IRIS's embedded-Python worker job mid-encode on this platform.
onnxruntime>=1.18.0
optimum[onnxruntime]>=1.20.0
requests>=2.31.0
pytest>=8.0.0
